# MinSpec

> Namespace anchor and future metapackage for MinSpec.

`minspec/minspec` exists to reserve and represent the canonical MinSpec Composer namespace.

MinSpec is an independent package-first Symfony composition project focused on disciplined application structure, LAST-stack defaults, and bounded AI/Mate developer tooling.

This repository is intentionally minimal.

It is not currently an implementation package.

---

## Purpose

This package currently serves as:

- the canonical Packagist namespace anchor for `minspec/*`
- the public Composer identity for the MinSpec project
- a future possible metapackage for curated MinSpec package installation

The package may later become a Composer metapackage that requires selected MinSpec packages such as UI, tooling, recipes, standards, or other installable components.

Until then, it should remain small and explicit.

---

## Current Status

MinSpec is in founder-controlled incubation.

This repository does not currently contain application code, bundle code, Symfony recipes, runtime implementation, or generated assets.

Functional implementation work belongs in the appropriate MinSpec repositories and packages.

---

## Not Symfony

MinSpec is an independent project built for Symfony applications.

It is not affiliated with, endorsed by, sponsored by, or maintained by Symfony SAS or the Symfony project.

Symfony is a trademark of Symfony SAS.

MinSpec should not be understood as:

- an official Symfony distribution
- a Symfony replacement
- a fork of Symfony
- a certification authority
- an autonomous AI app generator

MinSpec should be understood as:

> An independent, package-first composition project for building modern Symfony applications.

---

## Incubation Contribution Posture

MinSpec is public for visibility, review, reproducible feedback, and source-of-truth development.

Public visibility does not imply public governance, public write access, or an open contribution process. During incubation, source mutation remains restricted to the maintainer and explicitly trusted collaborators.

Pull requests are enabled but restricted to collaborators only. Only users with repository write, maintain, or admin access may open pull requests.

Collaborator PR access is an operational mechanism for trusted maintainers and approved collaborators. It is not a public contribution path.

During incubation, MinSpec is not accepting unsolicited external contributors, code pull requests, documentation pull requests, package submissions, recipe submissions, workflow changes, dependency changes, or AI-generated contribution patches.

Issues may remain available where enabled for questions, reproducible bug reports, documentation clarity suggestions, security-report routing, and design feedback. Opening an issue or providing feedback does not grant contribution authority, source authority, maintainer status, or approval to submit code changes.

AI agents, GitHub Apps, bots, automation, Dependabot, Copilot agents, browser agents, and external tools are not maintainers and do not gain source authority from collaborator-only PR settings.

See `CONTRIBUTING.md` for details.

---

## Related Repositories

- [`minspec/.github`](https://github.com/minspec/.github) — organization doctrine, governance, terminology, and planning
- [`minspec/skeleton`](https://github.com/minspec/skeleton) — canonical MinSpec project shell

Additional repositories may be created only when their package boundaries are clear and validated.
